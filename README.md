# rust-docker-quickstart-template

```shell
$ cargo install cargo-generate
```

```shell
$ cargo generate --git https://github.com/kevinjfields/rust-docker-quickstart-template --name my-project
$ cd my-project
```

```shell
$ docker-compose up
```
